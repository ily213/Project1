class TaskModel {
    constructor() {
        super()
    }

    // Получить задачи
    getTasks() {
        return
    }

    // Получить задачу по её ID
    getTaskById (id) {
        return
    }

    // Создать задачу
    createTask (task) {
        return task
    }

    // Обновить задачу
    updateTask (task) {
        return task
    }

    // Удалить задачу
    deleteTask (task) {
        return
    }
}

const taskModel = new TaskModel();
export default taskModel